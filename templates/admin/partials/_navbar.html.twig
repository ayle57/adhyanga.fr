<header class="border-bottom bg-white">
    <div class="container">
        <div class="d-flex align-items-center justify-content-between py-2">

            {# Logo #}
            <a href="{{ path('admin_dashboard') }}" class="d-flex align-items-center text-decoration-none">
                <img src="{{ asset('images/logo_admin.png') }}" width="36" height="36" alt="">
            </a>

            {# Nav #}
            <ul class="nav gap-2">
                <li class="nav-item">
                    <a href="{{ path('admin_dashboard') }}"
                       class="nav-link px-2 {{ currentRoute == 'admin_dashboard' ? 'fw-semibold text-primary' : 'text-muted' }}">
                        Utilisateurs
                    </a>
                </li>

                <li class="nav-item">
                    <a href="#"
                       class="nav-link px-2 {{ currentRoute == 'admin_pricing' ? 'fw-semibold text-primary' : 'text-muted' }}">
                        Rendez-vous
                    </a>
                </li>

                <li class="nav-item">
                    <a href="#"
                       class="nav-link px-2 {{ currentRoute starts with 'admin_faq' ? 'fw-semibold text-primary' : 'text-muted' }}">
                        Offres
                    </a>
                </li>

                <li class="nav-item">
                    <a href="#"
                       class="nav-link px-2 {{ currentRoute == 'admin_about' ? 'fw-semibold text-primary' : 'text-muted' }}">
                        Cartes Cadeaux
                    </a>
                </li>
            </ul>

            {# Logout #}
            <a href="{{ path('app_logout') }}" class="btn btn-sm btn-link text-danger text-decoration-none">
                Déconnexion
            </a>

        </div>
    </div>
</header>
